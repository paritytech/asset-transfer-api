import { ApiPromise } from "@polkadot/api";
import { MultiLocation } from '@polkadot/types/interfaces';

export class MultiLocationFactory {
    static createBeneficiary(
        api: ApiPromise
    ): MultiLocation {
        
    }

    static createDest(
        api: ApiPromise
    ) {

    }
}
